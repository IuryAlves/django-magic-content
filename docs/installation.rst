============
Installation
============

At the command line::

    $ easy_install django-magic-content

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-magic-content
    $ pip install django-magic-content
